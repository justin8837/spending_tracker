import React from "react";

const Card = ({ children }) => {
    return (
        <>
            <div class="AppCard">
                {children}
            </div>
        </>
    );
}
export default Card;
